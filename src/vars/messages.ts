const msgStartup = 'Welcome to the ${appName}';

const msgRequiredField = 'The following fields are required: ${fields}.';
const msgPageNotFound1 = 'Inscrutable are the 404 ways of improv.';
const msgPageNotFound2 =
  'The one you chose, however, is simply inexistent or has not been implemented yet.';

const msgMaxSuggestionAmount = 'The maximum amount of suggestions is ${amount}';

// Message templates
const msgTemplateInvalid = 'The string provided is not a valid template';
const msgTemplateArgs = 'The arguments passed are not valid';

// API requests
const msgBackendError = 'Error accessing the backend: ${errorMsg}.';

const msgAPIError =
  'The API request failed with error ${errorCode}: ${errorMsg}.';

export {
  msgStartup,
  msgRequiredField,
  msgPageNotFound1,
  msgPageNotFound2,
  msgMaxSuggestionAmount,
  msgTemplateInvalid,
  msgTemplateArgs,
  msgBackendError,
  msgAPIError
};
